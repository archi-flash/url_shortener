function submitForm(){  if(getShortLink($("#url").val())){  }else{      $("#result").html("Server error. Try again later.");  }}function getShortLink(url){                $.get("index.php",{url:url},function(data, status){      var result = JSON.parse(data);      if(result["status"]==1){          $("#result").html(result["url"]);          return true;      }else{          return false;       }         });    }